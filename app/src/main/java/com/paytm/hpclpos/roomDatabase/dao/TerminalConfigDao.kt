/*
package com.paytm.hpclpos.roomDatabase.dao

import androidx.room.*
import com.example.apphpcldb.entity.entity.Schema
import com.paytm.hpclpos.roomDatabase.entity.TerminalConfig

@Dao
interface TerminalConfigDao {
    @Insert
    fun insert(terminalConfig: TerminalConfig)

    @Update
    fun update(terminalConfig: TerminalConfig)

    @Delete
    fun delete(terminalConfig: TerminalConfig)

    @Query("delete from schema")
    fun deleteAllNotes()

    @Query("select * from ")
    fun getAllFromSchema() : List<TerminalConfig>

}*/
